import { DemandeDirective } from './demande.directive';

describe('SortableDirective', () => {
  it('should create an instance', () => {
    const directive = new DemandeDirective();
    expect(directive).toBeTruthy();
  });
});
